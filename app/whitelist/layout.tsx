import React from 'react';
import {Metadata} from "next";


export const metadata: Metadata = {
    title: 'Light Paris Rp [ Whitelist',
    description: 'Light Paris Rp [ Whitelist',
}


const WhitelistLayout = ({children}: {children: React.ReactNode}) => {
    return children
};

export default WhitelistLayout;
